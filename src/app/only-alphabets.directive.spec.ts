import { OnlyAlphabetsDirective } from './only-alphabets.directive';

describe('OnlyAlphabetsDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyAlphabetsDirective();
    expect(directive).toBeTruthy();
  });
});
